{
  "config": {
    "step": {
      "user": {
        "title": "HearthLight",
        "description": "This installs the HearthLight theme, sets it as the backend default, and registers the `custom:hearthlight-brand` dashboard card. All behaviors can be toggled later in the integration's options."
      }
    },
    "abort": {
      "single_instance_allowed": "HearthLight is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HearthLight options",
        "data": {
          "manage_theme": "Manage the theme file",
          "set_default_theme": "Set HearthLight as the backend default theme",
          "register_card_resource": "Register the brand card dashboard resource"
        },
        "data_description": {
          "manage_theme": "Keep themes/hearthlight/hearthlight.yaml installed and up to date. Local edits to that file are overwritten on update.",
          "set_default_theme": "Re-applies HearthLight for light and dark on every Home Assistant start.",
          "register_card_resource": "Makes custom:hearthlight-brand available on dashboards."
        }
      }
    }
  },
  "issues": {
    "themes_include_missing": {
      "title": "HearthLight theme is not being loaded",
      "description": "The theme file was installed, but Home Assistant is not loading it. Add a themes include to your configuration.yaml:\n\n```yaml\nfrontend:\n  themes: !include_dir_merge_named themes\n```\n\nThen restart Home Assistant."
    },
    "yaml_mode_resource": {
      "title": "Dashboard resource could not be registered automatically",
      "description": "Your dashboards are configured in YAML mode, so HearthLight loaded its card globally instead. For a cleaner setup, add the resource to your Lovelace YAML config:\n\n```yaml\nlovelace:\n  resources:\n    - url: /hearthlight/hearthlight-brand-card.js\n      type: module\n```"
    }
  }
}
